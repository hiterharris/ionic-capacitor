import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hhIonicCapacitor.app',
  appName: 'ionic-capacitor',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
